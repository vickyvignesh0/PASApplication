# Stage 1: Build Angular administrative portal
FROM node:20-alpine AS build
WORKDIR /app
COPY admin-config/package*.json ./
RUN npm install
COPY admin-config/ ./
RUN npm run build -- --configuration=production

# Stage 2: Serve using Nginx
FROM nginx:alpine
COPY --from=build /app/dist/admin-config/browser /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
